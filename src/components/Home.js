import React from "react";
const Home = props => (
    <main className="principal">
        <h2>Últimos lançamentos</h2>
        <div class="card">
            <p>Conteúdo da Home Page</p>
        </div>
    </main>
);

export default Home;